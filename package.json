{
  "name": "talent-acquisition-website",
  "version": "1.0.0",
  "description": "Talent Acquisition Company Website",
  "scripts": {
    "dev": "cd nextjs-client && npm run dev",
    "build": "cd nextjs-client && npm run build",
    "start": "cd nextjs-client && npm start",
    "server": "cd server && npm run dev",
    "client-legacy": "cd client && npm start",
    "install-all": "npm install && cd nextjs-client && npm install && cd ../server && npm install"
  },
  "keywords": ["talent", "acquisition", "recruitment"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
